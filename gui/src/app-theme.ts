const theme = {
  darkBackground: "#272a2e",
  lightBackground: "white",
  colorPrimary: "#00b3ec",
  palette: ["#0068b6", "white", "#231815"],
};

export type ITheme = typeof theme;

export default theme;
